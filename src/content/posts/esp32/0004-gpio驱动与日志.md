---
title: GPIO驱动与日志
published: 2026-08-14
description: ESP-IDF 课程第 4 课:把 STM32 的 HAL_GPIO 翻译成 IDF 的 gpio_config/gpio_set_level/gpio_get_level,顺便学会 ESP32 的看家日志 ESP_LOGx。
tags: [ESP32, 嵌入式, GPIO, 日志]
category: ESP32
draft: false
prevTitle: 定时器与延时
prevSlug: "esp32/0005-定时器与延时"
nextTitle: FreeRTOS任务与事件循环
nextSlug: "esp32/0003-freertos任务与事件循环"
---

# GPIO 驱动与日志

STM32 的 `HAL_GPIO_Init/WritePin/ReadPin` 你在行。IDF 的 GPIO 风格完全不同——**先配置后使用,配置靠结构体,函数是 `gpio_` 前缀的平铺 API**。这一课翻译到位,顺便学会 ESP32 的看家日志 `ESP_LOGx`。

## 知识点 1:配置即结构体

```c
// STM32:GPIO_InitTypeDef + HAL_GPIO_Init(GPIOB, &cfg);
// ESP32:同样"填结构体",但函数是 gpio_config()
gpio_config_t io = {
    .pin_bit_mask = 1ULL << GPIO_NUM_2,  // 注意是位掩码
    .mode = GPIO_MODE_OUTPUT,
    .pull_up_en = GPIO_PULLUP_DISABLE,
    .pull_down_en = GPIO_PULLDOWN_DISABLE,
    .intr_type = GPIO_INTR_DISABLE,
};
gpio_config(&io);
gpio_set_level(GPIO_NUM_2, 1);      // 输出高
gpio_set_level(GPIO_NUM_2, 0);      // 输出低
```

对照记忆:HAL 的 `Init/WritePin/ReadPin` ↔ IDF 的 `gpio_config / gpio_set_level / gpio_get_level`。每个外设(driver 组件)都是这个套路:xxx_config / xxx_init / xxx_读写。

## 知识点 2:日志系统(看代码第一工具)

```c
#include "esp_log.h"
static const char *TAG = "my_app";
ESP_LOGE(TAG, "错误: %d", err);   // 错误(红)
ESP_LOGW(TAG, "警告");            // 警告
ESP_LOGI(TAG, "信息");             // 普通
ESP_LOGD(TAG, "调试(需开日志级别)");
```

`idf.py monitor` 打开的就是这个日志流。**读开源代码第一件事:找 TAG,跟着 ESP_LOG 看执行流**——xiaozhi 里到处是 `ESP_LOGI(TAG,...)`。

## 知识点 3:改日志级别

```bash
idf.py menuconfig
# Component config → Log output → 默认级别调到 DEBUG,能看到 ESP_LOGD
```

## 动手实践

回 `basic_routines/00_basic/main/main.c`,把上一课留的作业对答案:GPIO 初始化、延时、日志三个 API 分别是 `gpio_config`、`vTaskDelay`、`ESP_LOGx`。然后在代码里加一行 `ESP_LOGI`,`idf.py -p /dev/ttyUSB0 flash monitor` 看输出。

## 测验

### 测验 1
gpio_config 里 .pin_bit_mask 填什么?
- A. 位掩码(正确)
- B. 引脚号
- C. 寄存器值

<details>
<summary>答案与解析</summary>

**答案：A**。1ULL << 引脚号,一次可配置多脚。
</details>

### 测验 2
读陌生工程代码,最先跟着什么走?
- A. 日志(正确)
- B. Makefile
- C. 启动地址

<details>
<summary>答案与解析</summary>

**答案：A**。ESP_LOGx,日志是执行流的索引。
</details>

## 推荐阅读(今天就做)

> **乐鑫官方《GPIO 与管脚矩阵》**([GPIO API](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/api-reference/peripherals/gpio.html))与《日志库》文档。对照你手上 00_basic 代码读一遍。

## 下一步

下一课:定时器与延时——`esp_timer`、`esp_timer_create` 这些"软件定时器",以及它们和 FreeRTOS ticks 的关系。

**有任何不懂的,直接问我——我是你的老师。**

| [← 上一课](/my-blog/posts/esp32/0003-freertos任务与事件循环/) | [课程目录](/my-blog/posts/esp32/00-总览/) | [下一课 →](/my-blog/posts/esp32/0005-定时器与延时/) |