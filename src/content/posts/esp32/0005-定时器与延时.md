---
title: 定时器与延时
published: 2026-08-15
description: ESP-IDF 课程第 5 课:三层时间体系——FreeRTOS ticks、esp_timer 软件定时器、gptimer 硬件定时器,分清它们才不在看 xiaozhi 代码时晕。
tags: [ESP32, 嵌入式, 定时器, esp_timer]
category: ESP32
draft: false
prevTitle: 中断与ISR
prevSlug: "esp32/0006-中断与isr"
nextTitle: GPIO驱动与日志
nextSlug: "esp32/0004-gpio驱动与日志"
---

# 定时器与延时

STM32 里你有 SysTick、TIM、HAL_Delay。ESP32 世界里有**三层"时间"**:FreeRTOS ticks、`esp_timer`、硬件定时器(gptimer)。分清它们,才不会在看 xiaozhi 代码时晕。

## 知识点 1:三层时间体系

| 机制 | 精度 | 用途 | STM32 类比 |
|------|------|------|------------|
| `vTaskDelay / pdMS_TO_TICKS` | ms 级 | 任务里"让位睡觉" | HAL_Delay |
| `esp_timer`(软件定时器) | µs 级 | 回调定时任务,不占任务栈 | TIM + 回调 |
| `gptimer`(硬件定时器) | ns 级 | 精确 PWM/测量 | TIM 高级定时器 |

## 知识点 2:esp_timer(最常用)

```c
static void tick_cb(void *arg) {
    ESP_LOGI(TAG, "tick!");          // 在 esp_timer 任务上下文执行
}
esp_timer_create_args_t args = { .callback = tick_cb };
esp_timer_handle_t t;
esp_timer_create(&args, &t);
esp_timer_start_periodic(t, 1000 * 1000);  // 每 1 秒(µs 为单位)
```

注意:`esp_timer` 回调跑在系统任务里,**不能做阻塞操作**——和 STM32 中断回调一个脾气。

## 知识点 3:测量时间戳

```c
int64_t now = esp_timer_get_time();  // µs 时间戳,常用于性能测量
```

## 动手实践

去 `basic_routines/05_esp_timer`(或 `06_gp_timer`)看例程:对照上面的三层体系,**说出这个例程用了几种"时间"机制、各是什么**。再看 `02_key` 例程,猜猜按键消抖为什么不用 HAL 式 delay。

## 测验

### 测验 1
esp_timer 回调里能做长时间阻塞吗?
- A. 不能(正确)
- B. 可以
- C. 随便

<details>
<summary>答案与解析</summary>

**答案：A**。不能,它跑在系统任务里,阻塞会卡住其他定时器。
</details>

### 测验 2
esp_timer_get_time 返回的单位是?
- A. 微秒(正确)
- B. 毫秒
- C. 时钟周期

<details>
<summary>答案与解析</summary>

**答案：A**。微秒(µs),int64_t 类型。
</details>

## 推荐阅读(今天就做)

> **乐鑫官方《esp_timer 文档》**([esp_timer](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/api-reference/system/esp_timer.html)):表格看"该用哪个定时器"那节。

## 下一步

下一课:中断与 ISR——GPIO 中断怎么注册、怎么从 ISR 安全地通知任务。

**有任何不懂的,直接问我——我是你的老师。**

| [← 上一课](/my-blog/posts/esp32/0004-gpio驱动与日志/) | [课程目录](/my-blog/posts/esp32/00-总览/) | [下一课 →](/my-blog/posts/esp32/0006-中断与isr/) |