---
title: 上下文管理器与装饰器
published: 2026-08-17
description: Pythonic 系列第 8 课（惯用法收尾）：with 自动释放资源，装饰器给函数外挂能力——@timed、@lru_cache 与 functools.wraps。
tags: [Pythonic, 上下文管理器, 装饰器, with, lru_cache]
category: Pythonic
draft: false
prevTitle: 时间与日期
prevSlug: "pythonic/0009-时间与日期"
nextTitle: 常用标准库
nextSlug: "pythonic/0007-常用标准库"
---

# 上下文管理器(with)与装饰器

这是 Pythonic 系列课程笔记的第 8 课，预计 25 分钟。这是收尾课,讲两个「C 里完全没有、Python 特有」的语法糖。`with` 替你管「用完要收拾」的资源(文件、连接、锁);**装饰器**让你不改函数本体给它加能力(计时、重试、日志)。两个都是 Python 生态里无处不在的关键词。

## 知识点 1:with——「用完自动关」,不用手动 close

C 思维(手动 open / 用完 close,还容易漏):

```python
f = open("data.txt")     # C:open-用-close
data = f.read()
f.close()                # 忘了怎么办?异常时更麻烦
```

Pythonic——`with` 进入时打开,离开时**自动调用 close**(即使中间报错也保证关闭):

```python
with open("data.txt") as f:
    data = f.read()      # 只要缩进块内,用完自动关
```

要点:凡是「有资源要释放」的对象(文件、锁、数据库连接、重定向),都用 `with ... as x:`。文件读写经常配合上一课的 pathlib:`Path.read_text()` 内部其实也是 with。

## 知识点 2:with 也能同时管多个 / 配合 generators

要同时读写两个文件、或开多个资源,`with A() as a, B() as b:` 一行搞定。

```python
with open("in.txt") as fin, open("out.txt", "w") as fout:
    fout.write(fin.read())
```

还有用 `@contextmanager` 把「先做A、finally做B」的逻辑变成 with 块——后面装饰器一同学到。

## 知识点 3:装饰器——不改原函数,给它包一层能力

C 程序员想给函数加计时,常改函数内部或复制代码。装饰器让你**外挂**一个「包装层」:`@timed` 只是把函数替换成包了一层的版本。

```python
import functools, time

def timed(func):
    @functools.wraps(func)   # 保留原名/文档,别让调试认不出原函数
    def wrapper(*args, **kwargs):
        t0 = time.perf_counter()
        result = func(*args, **kwargs)
        print(f"{func.__name__}: {time.perf_counter()-t0:.4f}s")
        return result
    return wrapper

@timed
def slow(x):
    return x * x

slow(5)      # 原函数一行没改,就多了计时
```

关键:`@timed` 等价于 `slow = timed(slow)`。装饰器就是一个「接收函数、返回新函数」的函数。

## 知识点 4:装饰器的常见用法

除了计时,AI/框架代码里到处都是:

```python
# 缓存(自动 memoize,免写手写缓存字典)
from functools import lru_cache
@lru_cache(maxsize=None)
def fib(n):
    return n if n < 2 else fib(n-1) + fib(n-2)

# 官方自带:注册去重 / 折叠
from functools import reduce, wraps
# 常用模式:日志、权限校验、重试、记时
```

`@lru_cache` 给纯函数加记忆化,是「免手写缓存」的头号例子;框架里 Flask 路由 `@app.route`、pytest 的 `@fixture` 也都是装饰器——现在你读 AI 代码能认出「这是在包一层」了。

## 知识点 5:什么时候别用

- with:对象不需要释放资源 → 别硬套;别用它把两件无关的事塞一块
- 装饰器:包装超过两三层 → 调试会过不了原函数;装饰器做了「悄悄的事」(缓存/重试)→ 记得写文档说明,否则同事懵

## 练习

打开 `practice/pythonic/0008_上下文管理器与装饰器.ipynb`:

- **练习 A**:把「open 后忘 close」的代码改成 with;写「读一个文件数行数,走 with」
- **练习 B**:写 @timed 装饰器;用 @lru_cache 加速斐波那契;写一个带参数的 @retry
- **练习 C**:训练 repo 里一段文件读写 => with 改写;一段重复计时/清理逻辑 => 装饰器/上下文管理

## 测验

### 测验 1
读文件,最省心写法?
- A. open 后手动 close
- B. with open(...) as f(正确)
- C. try/finally 手写
- D. Path 不管关

<details>
<summary>答案与解析</summary>

**答案：B**。with ... as f: 自动在离开块时关闭文件,异常也安全。
</details>

### 测验 2
@timed 实际等价于?
- A. timed(foo) 丢弃
- B. foo = timed(foo)(正确)
- C. foo 被复制
- D. timed 被替换

<details>
<summary>答案与解析</summary>

**答案：B**。@timed 是把 foo 替换成 timed 返回的包装函数,即 foo = timed(foo)。
</details>

### 测验 3
写装饰器最好保留原函数的?
- A. 内存地址
- B. 元信息(__name__ 等)(正确)
- C. 参数个数
- D. 源码文本

<details>
<summary>答案与解析</summary>

**答案：B**。用 @functools.wraps 保留原名和文档字符串,否则调试/文档认不出。
</details>

### 测验 4
免手写缓存/记忆化的现成装饰器?
- A. @timed
- B. @lru_cache(正确)
- C. @property
- D. @contextmanager

<details>
<summary>答案与解析</summary>

**答案：B**。functools.lru_cache 给纯函数加记忆化,自动缓存计算结果。
</details>

## 推荐阅读

> **Python 官方教程「[with 语句](https://docs.python.org/zh-cn/3/reference/compound_stmts.html#with)」**与「[装饰器](https://docs.python.org/zh-cn/3/glossary.html#term-decorator)」词条;再看 **Fluent Python** 的装饰器与上下文管理器两章(进阶,有空再读)。

## 完工了

这 8 课覆盖了 C 转 Python 的惯用法主线。**到这里,你已经从「把 C 翻译成 Python」走向「用 Python 的语序重说」。**回看课程地图,挑最不熟的一课回炉。

**有任何不懂的,随时问我——我是你的老师。**

| [← 上一课](/my-blog/posts/pythonic/0007-常用标准库/) | [课程目录](/my-blog/posts/pythonic/00-总览/) | [下一课 →](/my-blog/posts/pythonic/0009-时间与日期/) |