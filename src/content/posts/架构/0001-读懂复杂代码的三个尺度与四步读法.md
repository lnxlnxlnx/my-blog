---
title: 读懂复杂代码的三个尺度与四步读法
published: 2026-08-19
description: 架构与设计模式第 1 课:为什么复杂代码难读;函数/类/模块三个尺度;架构 vs 设计模式;GoF 三大类 22 式地图;四步读法实战拆解一段 AI 风格代码。
tags: [架构, Python, 设计模式, 读代码, 模板方法, 工厂方法]
category: 架构
draft: false
prevTitle: 接口与多态:ABC 和 Protocol
prevSlug: "架构/0002-接口与多态-abc和protocol"
nextTitle: ""
nextSlug: ""
---

# 读懂复杂代码的三个尺度与四步读法

你说「很多 AI 写的代码我都不是很懂」。这很正常——AI 生成的代码是为**扩展**写的,不是为**一口气读完**写的:它有抽象基类、有注册表、有工厂函数,把逻辑拆得到处都是。这节课给你一张地图和一套方法,让你以后拿到任何代码都能说出它的形状。

## 为什么复杂代码难读

复杂代码难读,通常因为三个原因叠加:

- **间接层多**:调用链弯弯绕绕(接口 → 抽象类 → 实现),你顺着调用链找,很容易迷路
- **命名抽象**:类名叫 `BaseTrainer`、`Handler`、`Registry`,不跑一遍不知道干嘛
- **模式不熟**:代码的「套路」你没见过,就不知道哪部分是骨架、哪部分是血肉

好消息:这三种难读,都有对应的解药。本节课解决「模式不熟」和「间接层多」。

## 第一个认知:代码有三个尺度

- **函数**:一个行为,几十行内。读它只看:输入、输出、副作用。
- **类**:一组状态 + 行为,几百行内。读它只看:有哪些字段、公开哪些方法。
- **模块/包**:一组类的协作,几千行以上。读它只看:入口在哪、谁依赖谁。

**看不懂,往往是因为你在一层想搞懂另一层的事。**比如:你非要读完每个函数才敢说「我懂这个模块」——其实你只需要模块层的答案(入口、依赖)。反过来,想改一个函数,却先去读整个包的依赖图——那也是浪费。先问自己:我现在在哪个尺度?

## 第二个认知:架构 vs 设计模式

- **架构(Architecture)**:模块之间怎么组织——谁在上层、谁在下层、依赖朝哪个方向。是代码库的「宏观形状」。
- **设计模式(Design Pattern)**:类与类之间协作的**经典套路**,有名字。是「微观套路」。

一句话记忆:**架构是地图,模式是招式。**地图决定你去哪,招式决定单挑时怎么出拳。本系列课程先练招式(设计模式),再学地图(架构),最后用两者去读真实项目。

## 设计模式地图:三大类 22 式

「设计模式」出自 GoF 四人的书,一共 22 个常用模式,按用途分三类([Refactoring Guru 目录](https://refactoring.guru/design-patterns/catalog),每个模式都有 Python 示例):

- **创建型 (Creational)**:怎么造对象:工厂方法、抽象工厂、建造者、原型、单例。
- **结构型 (Structural)**:类和对象怎么组合:适配器、桥接、组合、装饰器、门面、享元、代理。
- **行为型 (Behavioral)**:对象之间怎么分工协作:策略、观察者、模板方法、命令、状态、迭代器……

不用背,先混个脸熟。本课程只深入学最高频的七八个,其余的认得出、查得到就行。

## 技能:读代码四步法

面对陌生代码(尤其是 AI 生成的),按这四步走,顺序不能乱:

1. **找入口**:`main()`、脚本末尾、CLI 定义、测试里的调用处。入口是阅读的起点。
2. **画依赖**:顺着 `import` 和构造函数的参数,画出「谁依赖谁」。箭头方向就是信息流方向。
3. **找抽象**:标出接口/抽象基类(约定),和具体实现(兑现约定的地方)。骨架在这一步就清楚了。
4. **看实现**:只在你要改、要修的地方,才深入读具体类。

这四步的核心理念:**先看形状,再看血肉。**90% 的代码,你只需要前两步的答案。

## 实战:一段 AI 风格的代码

下面是一段典型的 AI 生成训练代码(和你的 ML 知识同一个领域,读起来最容易)。先用四步法拆,再看我的拆法:

```python
class BaseTrainer(ABC):
    """所有训练器的公共骨架。"""
    def run(self, data):            # 流程定死在抽象层
        x, y = self.preprocess(data)
        model = self.build()
        self.fit(model, x, y)
        return self.evaluate(model, x, y)

    @abstractmethod
    def preprocess(self, data): ...  # 四个钩子,子类填空
    @abstractmethod
    def build(self): ...
    @abstractmethod
    def fit(self, model, x, y): ...
    @abstractmethod
    def evaluate(self, model, x, y): ...

class TrainerRegistry:
    """把『名字 → 训练器类』登记起来。"""
    _registry = {}

    @classmethod
    def register(cls, name):
        def wrap(trainer_cls):
            cls._registry[name] = trainer_cls
            return trainer_cls
        return wrap

@TrainerRegistry.register("svm")
class SVMTrainer(BaseTrainer):
    def preprocess(self, data):
        return StandardScaler().fit_transform(data), ...
    def build(self):
        return SVC()
    def fit(self, model, x, y):
        model.fit(x, y)
    def evaluate(self, model, x, y):
        return model.score(x, y)

def make_trainer(name):              # 按名字造对象
    return TrainerRegistry._registry[name]()
```

四步拆法:

1. **入口**:`make_trainer("svm")` 造对象,`trainer.run(data)` 执行。
2. **依赖**:调用方只依赖 `BaseTrainer` 和 `TrainerRegistry`,**不**依赖 `SVMTrainer`。箭头指向抽象。
3. **抽象**:`BaseTrainer.run` 是骨架,4 个 `@abstractmethod` 是契约。
4. **实现**:`SVMTrainer` 只负责填空,registry 负责登记。

它用到的模式(先记住名字,细节后面逐课学):**模板方法**(Template Method,流程定在抽象类)、**工厂方法**(Factory Method,按名字造对象)、**注册表**(Registry,名字→类映射)。

> 关键洞见:读懂这段代码,你**不需要读 `SVMTrainer` 的任何一个方法**。抽象的功劳——这就是 AI 代码「绕」的原因,也是它值得学的原因。

## 练习

打开 `practice/architecture/0001_读懂复杂代码.ipynb`(JupyterLab 里运行),完成三个练习:

- **练习 A**:用四步法拆解上面这段代码,再对答案(答案直接写在文件里)
- **练习 B**:不修改任何现有类,新增一个逻辑回归训练器并注册进系统——体验「开闭原则」
- **练习 C**:选一个你最近用 AI 生成的代码文件,用四步法拆解,把结果写下来

## 测验

### 测验 1
「架构」关注的问题是?
- A. 类之间如何协作
- B. 模块间如何组织(正确)
- C. 函数内如何书写
- D. 变量如何来命名

<details>
<summary>答案与解析</summary>

**答案：B**。架构 = 模块之间怎么组织、依赖朝哪个方向;类之间的协作套路是设计模式管的事。
</details>

### 测验 2
设计模式的三大类是?
- A. 创建结构行为(正确)
- B. 输入输出处理
- C. 简单中等复杂
- D. 面向函数并行

<details>
<summary>答案与解析</summary>

**答案：A**。创建型(怎么造对象)、结构型(怎么组合)、行为型(怎么协作)。
</details>

### 测验 3
读懂陌生代码的第一步?
- A. 先找入口和依赖(正确)
- B. 逐行细读注释
- C. 先跑一遍代码
- D. 搜索全部报错

<details>
<summary>答案与解析</summary>

**答案：A**。先看形状(入口、依赖、抽象),再看血肉(实现)。逐行读是大忌——你会淹死在细节里。
</details>

## 推荐阅读

> **Refactoring Guru · Design Patterns 入门四篇**([中文版](https://refactoringguru.cn/design-patterns)):先读「什么是设计模式」「为什么要学模式」「模式的分类」三篇,再随便点开 [工厂方法](https://refactoringguru.cn/design-patterns/factory-method) 看一篇带 Python 示例的——感受一下「模式 = 有名字的套路」。

## 下一步

做完练习 C,回来告诉我:你拆的是哪个文件?四步法哪一步最卡?下一课进入**接口与多态**(Python 的 ABC 与 Protocol)——你会真正理解 `@abstractmethod` 和「依赖抽象」为什么存在,这是所有设计模式的地基。

**有任何不清楚的地方,随时问我——我是你的老师。**

| — | [课程目录](/my-blog/posts/架构/00-总览/) | [下一课 →](/my-blog/posts/架构/0002-接口与多态-abc和protocol/) |