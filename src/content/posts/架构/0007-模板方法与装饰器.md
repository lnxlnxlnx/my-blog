---
title: 模板方法与装饰器
published: 2026-08-25
description: 架构与设计模式第 7 课:模板方法骨架与钩子、默认钩子、装饰器包装与叠加、带参数装饰器,以及什么时候别用。
tags: [架构, Python, 设计模式, 模板方法, 装饰器]
category: 架构
draft: false
prevTitle: 组合优于继承与依赖注入
prevSlug: "架构/0008-组合优于继承与依赖注入"
nextTitle: 观察者模式:状态变了通知大家
nextSlug: "架构/0006-观察者模式-状态变了通知大家"
---

# 模板方法与装饰器

这课两个都叫「方法/装饰」的模式:一个你可能早就见过——第一课的 `BaseTrainer.run`;一个是 Python 最标志性的语法糖**装饰器**。两个都是 AI 代码里的高频面孔。

## 知识点 1:模板方法——骨架定在基类,钩子留给子类

**模板方法(Template Method)**:基类把「流程」写死,把流程中「会变的步骤」留成钩子,子类只填钩子([官方讲解](https://refactoringguru.cn/design-patterns/template-method))。你第一课就见过:

```python
from abc import ABC, abstractmethod


class BaseTrainer(ABC):
    def run(self, data):                     # ← 这就是「模板方法」:骨架
        x, y = self.preprocess(data)         #   流程定死,顺序不许改
        model = self.build()
        self.fit(model, x, y)
        return self.evaluate(model, x, y)

    @abstractmethod
    def preprocess(self, data): ...          # 钩子 1:子类填
    @abstractmethod
    def build(self): ...                     # 钩子 2
    @abstractmethod
    def fit(self, model, x, y): ...          # 钩子 3
    @abstractmethod
    def evaluate(self, model, x, y): ...     # 钩子 4


class SVMTrainer(BaseTrainer):               # 只填空,不碰骨架
    def preprocess(self, data): return scale(data)
    def build(self):       return SVC()
    def fit(self, model, x, y): model.fit(x, y)
    def evaluate(self, model, x, y): return model.score(x, y)
```

好处:所有子类**共享同一套流程**(保证「先清洗、再建模、再评估」的顺序不出错),又各自定制每一步。加一个新模型 = 新类填空,骨架零改动。

## 知识点 2:钩子还可以给默认实现

钩子不一定要抽象——给个默认实现,子类想改才覆盖:

```python
class BaseTrainer(ABC):
    def after_each_epoch(self, epoch, loss):   # 默认钩子:什么都不做
        pass

    def run(self, data):
        for epoch in range(10):
            loss = self.fit(data)
            self.after_each_epoch(epoch, loss)   # 子类可决定要不要覆盖


class LoggingTrainer(BaseTrainer):
    def after_each_epoch(self, epoch, loss):     # 只覆盖想改的那个钩子
        print(epoch, loss)
```

这也呼应了上一课的观察者——钩子是一种「写死的观察点」。区别:模板方法的钩子由**子类**实现;观察者的回调由**外部对象**注入。

## 知识点 3:装饰器——不改原函数,再包一层能力

**装饰器(Decorator)**是结构型模式在 Python 里的天生形态:在不改原函数代码的前提下,给它动态加上行为(计时、日志、缓存、权限……)([官方讲解](https://refactoringguru.cn/design-patterns/decorator))。

```python
import functools
import time


def timed(func):
    """装饰器:给函数加上『打印耗时』的能力。"""
    @functools.wraps(func)                  # 保真:保留原函数的名字/文档
    def wrapper(*args, **kwargs):
        t0 = time.perf_counter()
        result = func(*args, **kwargs)
        dt = time.perf_counter() - t0
        print(f"{func.__name__} 耗时 {dt*1000:.1f} ms")
        return result
    return wrapper


@timed                                   # 等价于 train_score = timed(train_score)
def train_score(model, X, y):
    model.fit(X, y)
    return model.score(X, y)


train_score(model, X, y)     # 原函数一行没变,就多了计时能力
```

关键点:`@timed` 只是把一个函数**替换**成包了一层的新函数。因为有了 `functools.wraps`,外面看起来还是原来的 train_score。

## 知识点 4:装饰器可以叠加,还能带参数

```python
@timed
@logged          # 多个装饰器 = 从下往上逐层包装
def load(path): ...


def retry(times=3):                      # 带参数的装饰器:多包一层工厂
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            for i in range(times):
                try:
                    return func(*args, **kwargs)
                except Exception:
                    if i == times - 1:
                        raise
            return None
        return wrapper
    return decorator


@retry(times=5)                          # 用法:给网络请求加自动重试
def fetch(url): ...
```

AI 代码和很多框架(Flask 路由、FastAPI、pytest.fixture、click 命令)都在大量用装饰器——认得出 @wraps 和「逐层包装」这个动作,你就能读懂它们了。

## 知识点 5:什么时候别用

- 模板方法:子类**只有一两个**、流程万年不变 → 直接一个函数写完更简单
- 装饰器:包装一层**超过两三层** → 调试时你会疯(看不到原函数了)
- 装饰器副作用:做了「悄悄的事」(重试、缓存)吗 → 记得文档里写清楚,否则同事会懵

## 练习

打开 `practice/architecture/0007_模板方法与装饰器.ipynb`:

- **练习 A**:写一个 Report 模板基类(标题→正文→落款三钩子),两个子类填空
- **练习 B**:写 timed / logged 两个装饰器并叠加;再加一个带参数的 retry
- **练习 C**:判断题:模板方法 vs 装饰器 vs 不需要(答案内联)

## 测验

### 测验 1
模板方法的核心是?
- A. 子类定义全部
- B. 运行时动态变化
- C. 基类定固定骨架(正确)
- D. 静态方法做重载

<details>
<summary>答案与解析</summary>

**答案：C**。骨架(流程)定在基类,会变的步骤留成钩子,子类只填空——所有子类共享同一套流程。
</details>

### 测验 2
装饰器函数的能力?
- A. 必须继承原函数
- B. 只能用在类方法
- C. 只能加一层包装
- D. 不改原函数而增强(正确)

<details>
<summary>答案与解析</summary>

**答案：D**。装饰器把函数「替换」成包了一层的新函数,原函数本体一行没改,还能多层叠加。
</details>

### 测验 3
写装饰器最好保留?
- A. 原函数的重载
- B. 对象的私有状态
- C. 调用的调用栈
- D. 原函数的元信息(正确)

<details>
<summary>答案与解析</summary>

**答案：D**。用 @functools.wraps 保留原名和文档字符串,否则调试和自动文档都会认不出原函数。
</details>

## 推荐阅读

> **Refactoring Guru · 模板方法**([中文版](https://refactoringguru.cn/design-patterns/template-method))与**装饰器**([中文版](https://refactoringguru.cn/design-patterns/decorator)):各读「问题-解决方案-结构」,再对照你自己 repopractice 里的训练循环,找找有没有现成的模板方法。

## 下一步

做完练习回来告诉我:练习 B 里你把计时和日志叠加后,调试变得多难受?下一课我们离开单个模式,进入**架构关键词:组合优于继承 + 依赖注入**——从「招式」迈向「内功」。

**有任何不懂的,直接问我——我是你的老师。**

| [← 上一课](/my-blog/posts/架构/0006-观察者模式-状态变了通知大家/) | [课程目录](/my-blog/posts/架构/00-总览/) | [下一课 →](/my-blog/posts/架构/0008-组合优于继承与依赖注入/) |