---
title: 组合优于继承与依赖注入
published: 2026-08-26
description: 架构与设计模式第 8 课:深继承链的问题、组合用「拥有」代替继承、依赖注入三种层次、DI 让测试脱胎换骨,以及什么时候才用继承。
tags: [架构, Python, 组合优于继承, 依赖注入, 测试]
category: 架构
draft: false
prevTitle: 分层架构与依赖方向
prevSlug: "架构/0009-分层架构与依赖方向"
nextTitle: 模板方法与装饰器
nextSlug: "架构/0007-模板方法与装饰器"
---

# 组合优于继承与依赖注入

前七课都是「招式」(设计模式)。从这课开始是「内功」——**不依赖具体模式、却贯穿所有代码的架构原则**。今天两条:组合优于继承、依赖注入。

## 知识点 1:继承的问题——基类一改,全链发抖

课本教我们用继承复用行为,但现实里深继承链是著名反模式。看这个越攒越深的金字塔:

```python
class BaseModel:
    def save(self): ...        # 基类:大家都要的
    def evaluate(self): ...


class SupervisedModel(BaseModel):     # 第二层
    def fit(self): ...


class TabularModel(SupervisedModel):  # 第三层
    def impute_missing(self): ...


class NNModel(TabularModel):          # 第四层……
    def backward(self): ...
```

问题链条:

- **脆**:改基类一个方法,所有后代都受影响,哪怕无关
- **表达力差**:一个模型可能「既是监督的、又能表格化、又能 NN」,多重分类只能用多继承,越搭越乱
- **测试难**:想测一个层级,得先构造一串祖先

## 知识点 2:组合——用「拥有」代替「继承」

**组合优于继承(Composition over Inheritance)**:一个对象**拥有**其他对象(作为字段),把行为委托给它们,而不是靠血缘关系(继承)获得行为。

```python
class Trainer:
    """组合:Trainer『拥有』一个模型 + 一个数据管道 + 一个报告器。"""
    def __init__(self, model, preprocessor, reporter):   # 依赖从外面来
        self.model = model          # 拥有
        self.preprocessor = preprocessor
        self.reporter = reporter

    def train_and_report(self, X, y):
        X_clean = self.preprocessor.fit_transform(X)
        self.model.fit(X_clean, y)
        self.reporter.emit(self.model)
```

现在想换预处理、换模型、换报告方式——**只换对象,不改 Trainer 的代码**。这就是「组合 = 可插拔」;而继承把一切焊死。

## 知识点 3:依赖注入——依赖从外面「传进来」

上面 `__init__(self, model, ...)` 把依赖从构造函数传进来,这就是**依赖注入(Dependency Injection, DI)**。反例是「内部 new」:

```python
class Trainer:
    def __init__(self):
        self.model = RandomForest()      # 坏:依赖在内部写死,换不了
        self.store = FileStore()         #      测试也没法替换


class Trainer:                            # 好:依赖从外面传
    def __init__(self, model, store):
        self.model = model
        self.store = store
```

DI 的三个层次(越往下越强):

- **构造器注入**:构造时传入(最常用,`__init__` 参数)
- **参数注入**:调用函数时传(`train(model, X, y)`)
- **属性注入**:之后 `trainer.model = ...`(灵活但不直观)

## 知识点 4:DI 让测试脱胎换骨

依赖从外部来,测试就能「偷梁换柱」——这正是工厂那课「注册假对象」的底气:

```python
class Trainer:
    def __init__(self, model, store):
        self.model = model
        self.store = store


def test_trainer_uses_fake_store():
    class FakeStore:                      # 测试专用的假依赖
        def __init__(self):
            self.saved = []
        def save(self, m):
            self.saved.append(m)

    fake = FakeStore()
    trainer = Trainer(FakeModel(), fake)  # 注入假的,不碰真实 IO/模型
    trainer.train(X, y)
    assert len(fake.saved) == 1          # 快速、无副作用、可断言
```

没有 DI 的话,测试里 new 出的真实模型和真实文件系统又慢、又不可控、还可能污染磁盘。

## 知识点 5:什么时候才用继承

「组合优于继承」不是说禁用继承。继承仍然合适在**真正的 is-a 关系 + 要复用基类的结构/默认行为**时——典型就是模板方法那课的 `BaseTrainer`:

- **用继承**:是-a 关系,子类要复用父类的流程/字段。例:所有 Trainer 共享 run 骨架。
- **用组合**:只是「需要某能力」而非「是一种」。例:Trainer 需要存储能力 → 拥有 store,而不是 extends Store。

经验法则:**「是一种」用继承,「拥有一个 / 需要一个」用组合**。80% 的场景答案是组合。

## 练习

打开 `practice/architecture/0008_组合优于继承与依赖注入.ipynb`:

- **练习 A**:把一个「继承辣条」式的模型改成「组合」版(拥有预处理 + 模型 + 报告器)
- **练习 B**:把内部 new 改成构造器注入,并用假依赖写一个测试
- **练习 C**:判断题:每个场景该继承还是组合(答案内联)

## 测验

### 测验 1
组合优于继承指?
- A. 永远不要用继承
- B. 用深继承树共享
- C. 把所有逻辑内聚
- D. 拥有依赖来复用(正确)

<details>
<summary>答案与解析</summary>

**答案：D**。让对象「拥有」其他对象、把行为委托给它们,而不是靠昂贵的遗传关系获得行为。
</details>

### 测验 2
依赖注入是指什么?
- A. 在内部 new 依赖
- B. 用单例共享依赖
- C. 禁用一切依赖项
- D. 从外部传入依赖(正确)

<details>
<summary>答案与解析</summary>

**答案：D**。依赖由外面(构造器/参数)传进来,而不是类内部写死;换依赖、测依赖都容易。
</details>

### 测验 3
继承适合真正的?
- A. 临时的行为复用
- B. 任意的相似结构
- C. 参数的便捷传递
- D. 本质的类别关系(正确)

<details>
<summary>答案与解析</summary>

**答案：D**。继承留给真正的「is-a」关系 + 共享结构/逻辑;只要「需要某能力」就优先组合。
</details>

## 推荐阅读

> **Refactoring Guru · 组合优于继承**([概览](https://refactoringguru.cn/design-patterns/factory-comparison)里的讨论):读「继承 vs 组合」对比。再看 Cosmic Python 的[目录](https://www.cosmicpython.com/)里「依赖注入」相关章节,提前感受真实项目的做法。

## 下一步

做完练习回来告诉我:练习 B 的假 store 测试跑通时,你有没有觉得「原来测试能这么干净」?下一课**分层架构与依赖方向**——把「单向依赖、高层依赖抽象」变成项目地图。

**有任何不懂的,直接问我——我是你的老师。**

| [← 上一课](/my-blog/posts/架构/0007-模板方法与装饰器/) | [课程目录](/my-blog/posts/架构/00-总览/) | [下一课 →](/my-blog/posts/架构/0009-分层架构与依赖方向/) |