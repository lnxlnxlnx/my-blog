# NOTES — 教学偏好记录

- **语言**：全程中文教学；代码注释用中文；html 页面用 UTF-8。
- **互动反馈**：用户基础好（C 熟练 + STM32 熟练），练习直接上真机（STM32F407 + 320×240 触摸屏），不搞模拟器优先。
- **教材对齐**：课程章节引用《LVGL 开发指南 V1.5》页码，用户手上有 PDF，方便对照翻阅。
- **工程状态**：LVGL v8.3.11 已移植（`EXTERNAL/LVGL/porting/`），`Core/Src/main.c` 已调用 `lv_init()`、`lv_port_disp_init()`；SysTick 中断里 `lv_tick_inc(1)`。课程代码都基于这个工程，`BSP/` 下已有 gui_menu.c、lvgl_2048.c 等用户自己写的小游戏。
- **课程定位**：目标是"完整产品界面"，部件篇不必逐章全学，按产品场景挑选核心部件。
- **风格**：页面可用少量 emoji 做视觉引导（见仓库 AGENTS.md），代码本身不加。
- **代码块配色（2026-08-27 定型）**：VSCode Light+ 色板，**行内 style 形式**（`<span style="color:#0000FF">`），白底细边框，不依赖任何 CSS 文件，任何环境渲染一致。由 `tools/prehighlight.js` 静态预渲染，新增/修改课程后重跑该脚本即可。
- 2026-08-27：课程体系定型为 12 课（见 index.html），第 1 课与术语表已交付。